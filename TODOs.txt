1. "exit" prints twice.
2. If there's a file named "ls" in curr_dir, the command "ls" will try to execute that file instead of the command.
3. When a command fails. Multiple "exit" are required to exit minishell.

